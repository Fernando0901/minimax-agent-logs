# Fix Report — minimax-agent container permanently unhealthy due to missing procps package

---

## 1. Problema Reportado

El monitor de salud proactivo detectó 15 errores recurrentes en las últimas 2 horas, todos del tipo `CONNECTION_ERROR — ReadTimeout`, provenientes de las comprobaciones de inicio (`startup checks`) del `odoo_bridge`. Específicamente, los endpoints `update_opportunity`, `agent_chat`, `create_quotation` y otros devolvieron errores de conexión y timeout.

Tras investigar más a fondo, se descubrió que **el contenedor `minimax-agent` estaba en estado `unhealthy`** con un `FailingStreak: 14` — es decir, había fallado 14 comprobaciones de salud consecutivas de Docker. El contenedor informaba repetidamente `HEALTHCHECK_FAILED: no python main.py process` cada 30 segundos, a pesar de que el proceso de Python estaba ejecutándose correctamente.

---

## 2. Diagnóstico del Pipeline

El pipeline de 7 agentes analizó el problema de la siguiente manera:

- **Agent 1 (Failure Analyst):** Clasificó el error como `INTEGRATION_ERROR` con confianza 0.82. Identificó que el contenedor `minimax-agent` reportaba estado `unhealthy` con `FailingStreak: 14`. Los errores de Odoo (`update_opportunity` HTTP 500, `create_quotation` HTTP 409, etc.) fueron identificados como síntomas secundarios, no como causa raíz.

- **Agent 2 (Root Cause Finder):** Ejecutó un análisis profundo de los archivos candidatos. Verificó mediante `docker exec minimax-agent pgrep` que `pgrep` no existía en el contenedor (exit code 127 = command not found). También confirmó que `docker top minimax-agent` mostraba el proceso `python -m agent.main` ejecutándose correctamente. El Dockerfile no incluía el paquete `procps`, que es el que proporciona `pgrep`. Además, encontró un problema secundario: el patrón `python.*main.py` en `healthcheck.sh` no coincidía con `python -m agent.main` porque en grep regex los puntos no coinciden con puntos literales sin escapar.

- **Agent 3 (Implementation Planner):** Diseñó un plan de 5 pasos atómicos con revertir completo, incluyendo: adición de `procps` al Dockerfile, validación de `pgrep` al inicio en `main.py`, mejora de observabilidad en `healthcheck.sh`, suite de pruebas, y rebuild del contenedor.

- **Agent 4 (Executor):** Ejecutó los 5 pasos en secuencia. Todos los archivos fueron respaldados antes de cualquier modificación. El contenedor fue reconstruido exitosamente.

- **Agent 5 (Results Auditor):** Recolectó evidencia en vivo: contenedor `minimax-agent` corriendo y `healthy`, `FailingStreak: 0` (reiniciado tras el rebuild), 6 entradas consecutivas de `HEALTHCHECK_PASSED` en `healthcheck.log`, 5/5 pruebas pasando. Veredicto: **PASS con confianza 0.97**.

- **Agent 6 (Decision Maker):** Decisión: **KEEP**. Confianza 0.97 (bien por encima del umbral de 0.7). Se actualizó PostgreSQL con el registro de la sesión, el patrón de error y el aprendizaje. Los errores de Odoo pre-existentes fueron programmados para seguimiento posterior.

---

## 3. Causa Raíz

**Archivo:** `/root/minimax-agent/Dockerfile` (líneas 6-10)
**Mecanismo:** El Dockerfile instalaba `curl`, `git`, `build-essential` pero **no incluía `procps`**, el paquete que proporciona `pgrep`, `ps`, `top`, `kill` y otras utilidades de procps en Debian/Ubuntu.

El script `healthcheck.sh` (introducido en la sesión `20260412_185955`) usa en CHECK 3 el comando:
```bash
PYTHON_PROCS=$(pgrep -f "python.*main.py" 2>/dev/null | wc -l || echo "0")
```

Cuando `pgrep` no existe (exit code 127 del command-not-found), el operador `||` activa el fallback `echo "0"`, así que `PYTHON_PROCS=0`. El script conclude incorrectamente que ningún proceso Python está corriendo, sale con código 1, y **Docker marca el contenedor como unhealthy cada 30 segundos** — aunque el proceso sí está ejecutándose.

**Causa raíz secundaria:** El patrón `python.*main.py` en grep regex no coincide con `python -m agent.main` porque el punto en regex coincide con cualquier carácter, no solo con puntos literales. El proceso se llamaba `python -m agent.main` (con un espacio y un flag `-m`), no `python.main.py`. El patrón debería ser `agent.main`.

**Causa de la causa:** La sesión `20260412_185955` introdujo el `healthcheck.sh` pero nunca actualizó el Dockerfile para incluir la dependencia `procps`. Esta es una brecha de dependencia faltante — el script de healthcheck fue añadido pero su requerimiento a nivel de sistema nunca fue propagado al Dockerfile.

---

## 4. Qué se Corrigió

### Cambio 1: Dockerfile — Añadir `procps`
Se añadió `procps` a la línea de `apt-get install` en el Dockerfile (línea 10). Esto asegura que `pgrep`, `ps`, `kill`, `top` y otras utilidades estén disponibles dentro del contenedor en todas las construcciones futuras.

### Cambio 2: main.py — Validación al inicio de `_validate_healthcheck_dependencies()`
Se añadió la función `_validate_healthcheck_dependencies()` en `agent/main.py` (línea 1353), llamada durante la validación al inicio (línea ~1202). Esta función usa `shutil.which("pgrep")` para verificar que el binario `pgrep` existe. Si no se encuentra, lanza `RuntimeError` con un mensaje CRITICAL, fallando el inicio inmediatamente en lugar de esperar a que el healthcheck falle cada 30 segundos.

### Cambio 3: healthcheck.sh — Mejorar observabilidad y patrón
Se reescribió el bloque CHECK 3 para:
- Detectar exit code 127 (pgrep no encontrado) vs exit code 1 (no hay coincidencias) vs exit code 0 (proceso encontrado)
- Cambiar el patrón de `python.*main.py` a `agent.main`, que sí coincide correctamente con `python -m agent.main`

### Cambio 4: Suite de pruebas `tests/test_healthcheck_dependencies.py`
Se creó un archivo de pruebas con 5 tests cubriendo:
1. `test_pgrep_binary_available()` — verifica que `shutil.which("pgrep")` devuelve una ruta
2. `test_healthcheck_script_executable()` — verifica que `healthcheck.sh` es ejecutable
3. `test_healthcheck_pgrep_check()` — ejecuta healthcheck.sh y verifica exit 0 cuando el proceso corre
4. `test_no_false_unhealthy_when_process_running()` — confirma que el contenedor no es falsamente marcado como unhealthy
5. `test_healthcheck_log_shows_passed()` — verifica entradas `HEALTHCHECK_PASSED` en el log

### Cambio 5: Rebuild del contenedor
Se ejecutó `docker compose up --build -d` para reconstruir el contenedor `minimax-agent` con el Dockerfile actualizado. El contenedor transicionó de `starting` (30s) → `healthy`. `FailingStreak` se reinició a 0.

---

## 5. Mejoras Estructurales Añadidas

1. **Validación al inicio (`startup check`):** `_validate_healthcheck_dependencies()` detecta la falta de `pgrep` antes de que el healthcheck de Docker falle, proporcionando un mensaje CRITICAL claro e inmediato en los logs en lugar de 30 segundos de silencio.

2. **Observabilidad mejorada en `healthcheck.sh`:** El script ahora distingue entre tres casos: `pgrep` no existe (exit 127), no hay proceso coincidente (exit 1 con 0 cuentas), y proceso encontrado (exit 0 con N>0 cuentas). Cada caso tiene un mensaje de log diferente.

3. **Suite de pruebas automatizadas:** `tests/test_healthcheck_dependencies.py` con 5 tests que habrían detectado la dependencia faltante de `procps` antes de producción.

4. **Patrón de grep corregido:** El patrón `agent.main` es más preciso y no depende de la coincidencia de puntos en regex.

---

## 6. Verificación

| Métrica | Antes del Fix | Después del Fix |
|---------|-------------|----------------|
| Estado del contenedor | `unhealthy`, `FailingStreak: 14` | `healthy`, `FailingStreak: 0` |
| `pgrep` disponible | `pgrep: not found` (exit 127) | `procps-ng 4.0.4` |
| `healthcheck.log` | 25+ entradas `HEALTHCHECK_FAILED` consecutivas | 6+ entradas `HEALTHCHECK_PASSED` consecutivas |
| Pruebas | Suite no existía | 5/5 PASS |
| Errores nuevos introducidos | baseline | 0 errores nuevos |
| Patrón pgrep | `python.*main.py` (no coincide) | `agent.main` (coincide correctamente) |

**Verificación semántica de archivos:**
- `/root/minimax-agent/Dockerfile` — `procps` añadido en línea 10 ✅
- `/root/minimax-agent/agent/main.py` — `_validate_healthcheck_dependencies()` añadida en línea 1351, llamada en línea ~1202 ✅
- `/root/minimax-agent/scripts/healthcheck.sh` — CHECK 3 reescrito con detección de exit 127 + patrón `agent.main` ✅
- `/root/minimax-agent/tests/test_healthcheck_dependencies.py` — suite de 5 tests creada y pasando ✅

---

## 7. Issues Pre-existentes Encontrados

Los siguientes problemas fueron identificados durante el análisis pero **no son parte de este fix** — son problemas pre-existentes del backend de Odoo que deben ser manejados en sesiones separadas:

| Issue | Severidad | Estado |
|-------|-----------|--------|
| Odoo backend returning HTTP 500 on `update_opportunity` (Fault 2: Record does not exist) | MED | Pre-existente — requiere investigación del endpoint XML-RPC de Odoo |
| Odoo backend returning HTTP 500 on `schedule_activity` (ACL restriction) | MED | Pre-existente — verificar permisos ACL de Odoo para creación de actividades |
| Odoo backend returning HTTP 409 on `create_quotation` (AMBIGUITY) | MED | Pre-existente — investigar prevención de cotizaciones duplicadas |
| Odoo connection pool exhaustion (7/5 in use) | LOW | Pre-existente — monitor de uso; ya tiene circuit breaker |
| `healthcheck.log` muestra advertencias de connection pool de Odoo | LOW | Pre-existente — no relacionado con este fix |

---

## 8. Rendimiento del Pipeline

| Etapa | Tiempo | Notas |
|------|--------|-------|
| Agent 1 (Failure Analyst) | ~2 min | Análisis de trayectorias de fallo + clasificación |
| Agent 2 (Root Cause) | ~2 min | Análisis profundo con verificación `docker exec` |
| Agent 3 (Planner) | ~2 min | Plan de 5 pasos con revertir completo |
| Agent 4 (Executor) | ~5 min | 4 edits + rebuild (~90s build) |
| Agent 5 (Auditor) | ~2 min | Recolección de evidencia en vivo + veredicto |
| Agent 6 (Decision) | ~1 min | Decisión KEEP + PostgreSQL update |
| Agent 7 (Debug Approver) | ~2 min | Este reporte + actualización CLAUDE.md |
| **Total** | **~16 min** | Dentro del presupuesto de 20 min |

---

## 9. Respaldos Disponibles

Los siguientes respaldos fueron creados por Agent 4 antes de cada modificación:

| Archivo Original | Ruta del Respaldo | Tamaño |
|------------------|-------------------|--------|
| `/root/minimax-agent/Dockerfile` | `/root/agent-tasks/backups/20260412_193251/Dockerfile_20260412_193251.bak` | 987 bytes |
| `/root/minimax-agent/agent/main.py` | `/root/agent-tasks/backups/20260412_193251/main.py_20260412_193251.bak` | 62,738 bytes |
| `/root/minimax-agent/scripts/healthcheck.sh` | `/root/agent-tasks/backups/20260412_193251/healthcheck.sh_20260412_193251.bak` | 3,746 bytes |

**Comando de revertir completo:**
```bash
cp /root/agent-tasks/backups/20260412_193251/Dockerfile_20260412_193251.bak /root/minimax-agent/Dockerfile
cp /root/agent-tasks/backups/20260412_193251/main.py_20260412_193251.bak /root/minimax-agent/agent/main.py
cp /root/agent-tasks/backups/20260412_193251/healthcheck.sh_20260412_193251.bak /root/minimax-agent/scripts/healthcheck.sh
rm /root/minimax-agent/tests/test_healthcheck_dependencies.py
cd /root/minimax-agent && docker compose up --build -d
```

**Nota:** El revertir NO es necesario — el fix fue verificado como PASS con confianza 0.97.

---

## Resumen

El contenedor `minimax-agent` estaba permanentemente en estado `unhealthy` porque el `Dockerfile` no incluía el paquete `procps`, que proporciona `pgrep`. El script `healthcheck.sh` dependía de `pgrep` para detectar si el proceso de Python estaba corriendo, pero como `pgrep` no existía, cada healthcheck reportaba falsamente que no había proceso y Docker marcaba el contenedor como unhealthy.

El fix añadió `procps` al Dockerfile, añadió una validación al inicio en `main.py`, mejoró la observabilidad en `healthcheck.sh`, y creó una suite de pruebas de 5 tests. El contenedor ahora está `healthy` con `FailingStreak: 0` y 6+ entradas consecutivas de `HEALTHCHECK_PASSED` en el log.

Los errores de Odoo (HTTP 500 en `update_opportunity` y `schedule_activity`, HTTP 409 en `create_quotation`) son problemas pre-existentes del backend de Odoo, no relacionados con este fix, y deben ser investigados en sesiones separadas.
